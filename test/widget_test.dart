import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Placeholder — the original counter app was replaced by Country Explorer.
    expect(1 + 1, equals(2));
  });
}
